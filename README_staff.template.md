<!--
  ШАБЛОН технического README для команды поддержки (README_staff.md).
  НЕ публикуется для конечных пользователей. Хранится в репозитории.
  Исполнитель: скопируй в README_staff.md и поддерживай в актуальном состоянии.
  Язык — русский. Тон — для инженера поддержки и разработки.
-->

# README_staff — техническая документация ProgressPrivate

> Внутренний документ для команды поддержки и разработки.
> Обновляется при каждом значимом изменении архитектуры или функций.

---

## 1. Назначение приложения

ProgressPrivate — privacy-first мобильное приложение для прогресс-фотографий.
Все данные хранятся локально. Нет сети для пользовательского контента,
нет рекламы, нет аналитики, нет облачного ML.

Ключевые продуктовые ограничения зафиксированы в CONSTITUTION.md
и являются обязательными.

---

## 2. Технологический стек

Фактические версии — в STACK_RESOLVED.md (источник истины).

- Framework: Expo (актуальный стабильный SDK)
- Runtime: React Native, новая архитектура
- Навигация: Expo Router (file-based)
- Анимации: react-native-reanimated
- Жесты: react-native-gesture-handler
- UI: gluestack-ui
- Состояние: Zustand
- Локальное хранилище метаданных: react-native-mmkv (с шифрованием)
- Файлы фото: expo-file-system (sandbox)
- Биометрия: expo-local-authentication
- Камера: expo-camera
- Уведомления: expo-notifications
- Отображение фото: expo-image

---

## 3. Архитектура

Полное описание — в ARCHITECTURE.md. Кратко:

```
/app         — экраны (Expo Router)
/src
  /components — переиспользуемые UI-компоненты
  /features   — фичи по доменам (camera, projects, gallery, settings, ...)
  /models     — TypeScript-типы
  /store      — Zustand-сторы
  /storage    — MMKV, SecureStore, файловая система
  /theme      — токены дизайна
  /utils      — чистые функции
```

### Границы (критично для приватности)

- Работа с файлами и MMKV — ТОЛЬКО в /src/storage.
- Сетевого слоя (/src/api, /src/network) быть НЕ должно.
- UI не обращается к файловой системе напрямую.

---

## 4. Модель данных

[ЗАПОЛНИТЬ по мере реализации. Ориентировочно:]

- Project: id, name, createdAt, coverPhotoId
- PhotoMetadata: id, projectId, uri (локальный путь), createdAt
- AppSettings: theme, ghostOpacity, requireBiometrics, reminders

Метаданные — в MMKV (зашифровано). Файлы фото — в sandbox приложения.

---

## 5. Приватность: как устроено технически

- Encryption key для MMKV хранится в SecureStore.
- Фото сохраняются в приватную директорию приложения, НЕ в галерею.
- Экспорт в галерею — только по явному действию пользователя.
- Приложение не делает сетевых запросов с пользовательским контентом.
- Проверяемость: сетевой трафик не должен содержать фото/метаданные.

Как проверить приватность:
1. Запустить приложение в авиарежиме — основные функции работают.
2. Через сетевой прокси убедиться, что фото не уходят наружу.
3. Проверить отсутствие рекламных/аналитических SDK в зависимостях.

---

## 6. Как собрать и запустить

### Требования окружения

- Node.js LTS, Git
- Xcode (iOS) и/или Android Studio (Android)
- EAS CLI (`npm install -g eas-cli`)

### Установка

```bash
npm install
```

### Запуск (development build, не Expo Go)

```bash
npx expo run:ios
npx expo run:android
```

### Проверки

```bash
npm run typecheck
npm run lint
npm test
npx expo-doctor
```

---

## 7. Тестирование

Стратегия и правила — в TESTING.md. Кратко:

- Unit-тесты покрывают store, storage, utils, models.
- Camera, биометрия, жесты, уведомления — ручная проверка.
- Тесты запускаются командой `npm test`.

---

## 8. Сборка и публикация

[ЗАПОЛНИТЬ конкретикой после настройки EAS]

- Development build: EAS Build (профиль development).
- Production build: EAS Build (профиль production).
- OTA-обновления: EAS Update (по необходимости).
- Публикация: App Store Connect и Google Play Console.

Требуется:
- аккаунт Apple Developer;
- аккаунт Google Play Console;
- корректные permission-тексты в app.json;
- заполненная Privacy Nutrition Label в App Store.

---

## 9. Типичные проблемы и их решение

| Симптом | Вероятная причина | Решение |
|---------|-------------------|---------|
| Краш при старте на MMKV | Запуск в Expo Go | Использовать development build |
| Камера крашит при запросе | Нет permission-текста | Добавить NSCameraUsageDescription в app.json |
| Haptic не срабатывает | Запуск в симуляторе | Проверять на реальном устройстве |
| expo-doctor ругается на версии | Ручная установка пакета | Ставить через `npx expo install` |
| Фото не сохраняются | Ошибка в storage-слое | Проверить /src/storage/photoFiles.ts |

[ЗАПОЛНИТЬ по мере накопления реальных инцидентов]

---

## 10. Что нельзя делать при поддержке

- Добавлять сетевые запросы с фото или метаданными.
- Добавлять рекламные или аналитические SDK.
- Включать облачный бэкап по умолчанию.
- Сохранять фото в галерею без явного действия пользователя.
- Менять версии из STACK_RESOLVED.md без проверки совместимости.

Любое такое изменение требует отдельного решения и записи в DECISIONS.md.

---

## 11. Контакты и ответственность

[ЗАПОЛНИТЬ: кто отвечает за релизы, кто за поддержку, каналы связи]

---

## 12. Связанные документы

- CONSTITUTION.md — неизменяемые правила
- ARCHITECTURE.md — границы слоёв
- STACK_RESOLVED.md — актуальные версии
- TESTING.md — протокол тестов
- DECISIONS.md — журнал решений
- MAINTENANCE_AI.md — инструкция для ИИ поддержки и развития
