/**
 * Назначение: конфигурация рекламных placeholder-слотов (dev-превью).
 *
 * Функции:
 * - SHOW_AD_PLACEHOLDERS: глобальный флаг показа тестовых мест под рекламу.
 *
 * Слой: feature (/src/features/ads). Без логики и зависимостей.
 *
 * ВАЖНО: это ТОЛЬКО визуальные заглушки под будущий Yandex Mobile Ads SDK.
 * Реальный SDK здесь НЕ подключается. Перед публикацией без рекламы поставьте
 * флаг в `false`, чтобы заглушки не показывались пользователям.
 */
export const SHOW_AD_PLACEHOLDERS = true;
